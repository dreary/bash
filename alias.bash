# -----------------------------------------------------
# Aliases
# -----------------------------------------------------

alias la='ls -AF'
alias ll='ls -al'

alias c='clear'
alias h='history'
alias x='exit'

alias _='sudo'

alias edit="$EDITOR"
alias pager="$PAGER"

alias reload='source ~/.bashrc'

alias '~'='cd ~/'
alias ..='cd ../'
alias ...='cd ../../'
